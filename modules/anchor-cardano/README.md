# Module placeholder

This folder will contain a replaceable module implementation.

MVP note:
- Core protocol lives in Rust (`core/acta-core`).
- Modules implement interfaces in services/tools; they must not redefine truth.
