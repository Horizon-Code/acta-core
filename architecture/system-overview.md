# System Overview

ACTA separates:

- Core protocol primitives and invariants (`core/`).
- Domain semantics through profiles (`profiles/`).
- Decisions that freeze boundaries (`decisions/`).

Architecture is subordinate to directional foundations, and implementation is subordinate to architecture.
